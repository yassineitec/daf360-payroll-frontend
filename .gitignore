# Compiled output
/dist
/tmp
/out-tsc

# Node
/node_modules
npm-debug.log
yarn-error.log

# IDEs
.idea/
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# Misc
/.angular/cache
.sass-cache/
/connect.lock
/coverage
/libpeerconnection.log
testem.log
/typings
.yarn/

# System Files
.DS_Store
Thumbs.db

# Credentials — never commit
src/environments/environment.local.ts
# Holds the GitHub Packages PAT for @khalilrebhiitec/daf360. Committing it gets
# the token auto-revoked by GitHub secret scanning, which breaks every project's
# npm install and Docker build — not just this one.
.npmrc
